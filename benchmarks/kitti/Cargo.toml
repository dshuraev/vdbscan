[package]
name = "vdbscan-kitti-bench"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
vdbscan = { path = "../.." }

[dev-dependencies]
criterion = "0.5.1"

[[bench]]
name = "kitti"
harness = false
